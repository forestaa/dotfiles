name:                xmonad-config
version:             0.1.0.0
github:              "forestaa/dotfiles/xmonad"
license:             BSD3
author:              "Daichi Morita"
maintainer:          "t.daichi.3@gmail.com"
copyright:           "2018 Daichi Morita"

extra-source-files:
- README.md
- ChangeLog.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/forestaa/dotfiles/xmonad#readme>

dependencies:
- base >= 4.7 && < 5

library:
  source-dirs: src

executables:
  xmonad-exe:
    main:                xmonad.hs
    source-dirs:         app
    dependencies:
    - xmonad
    - xmonad-contrib 
    - mtl
    - X11

